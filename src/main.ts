import "./store/initializer";
import {
  initAbout,
  initMetricsStats,
  initCostStats,
  initDeployStats,
} from "./web-components";

initAbout();
initMetricsStats();
initCostStats();
initDeployStats();
